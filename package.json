{
  "name": "parcel_beginner",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "parcel src/index.html",
    "build": "parcel build src/index.html",
    "deploy": "rm -rf dist/ && npm run build && gh-pages -d dist"
  },
  "homepage": "https://.github.io/",
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "parcel": "^2.8.0"
  },
  "dependencies": {
    "lodash": "^4.17.21",
    "gh-pages": "^4.0.0"
  }
}
